# SimpleAuthWithNodeAndPassport

## Installation
- Install package dependencies using `npm install`
- Run server using `npm start` 
- browse at http://localhost:8080 

## Login credentials
 username: `sg`
 password: `secret`
